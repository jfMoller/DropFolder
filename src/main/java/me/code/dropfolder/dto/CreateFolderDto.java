package me.code.dropfolder.dto;

/**
 * Record representing the data transfer object (DTO) for creating a new folder.
 *
 * @param name The name of the folder to be created.
 */
public record CreateFolderDto(String name) {
}
